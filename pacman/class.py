class Movable:
    """Classe définissant les entitées capables de se déplacer carractérisée par:
        -sa position x
        -sa position y
        """

    def __init__(self):
        """Par défaut """"

    def __deplacement__(test,x,test) :

      if ypos == 1 or ypos == 10 or xpos == 10 xpos == 1 :
          mov = 10
      else mov = 1
      if deplacement==up) and (chaine[x-12]==test) :
          dep="y+"
          rep=true
      if (deplacement==down) and (chaine[x+12]==test) :
          dep="y-"
          rep=true
      if (deplacement==left) and (chaine[x-1]==test)
          dep="x+"
          rep=true
      if (deplacement==right) and (chaine[x+1]==test) :
          dep="x-"
          rep=true
      else:
          rep=false



class Gohst(Movable):
    def __init__(self):
        "Par défaut les phantomes n'ont peur de rien"


class PacMan(Movable):
    def __init__(self):
        "Par défaut pacman à peur des phantomes"
        x =
        fear = 'n'
